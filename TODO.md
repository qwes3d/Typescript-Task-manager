# TODO

- [ ] Fix `tsconfig.json` so `npx tsc` emits `dist/app.js` (add `outDir`, `rootDir`, `include`).
- [ ] Run `npx tsc` and verify `dist/app.js` exists.
- [ ] Ensure HTML pages reference the correct built JS output.
- [ ] Re-test by opening `Pages/index.html` and checking the app logs / UI.

